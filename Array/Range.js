// declare an empty Array
var numbers = [];
// Take a Range from 1 to 100
for (var i = 0; i <= 100; i++) {
    if( i != 0) {
        if(i%11 ==  0) {
         //Store the elements in an Array
        numbers[i] == i;
        console.log(i);
       }
    }
}
